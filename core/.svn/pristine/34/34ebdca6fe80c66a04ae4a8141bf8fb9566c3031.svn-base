<?php

namespace core\helper;

class BadgeHelper {

    static public function aplicar($valor, $cor) {
        $cor = !empty($cor) ? 'bg-' . $cor : '';
        return '<span class="badge ' . $cor . '">' . $valor . '</span>';
    }

}
